{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@features/*": ["./features/*"],
    },
  },
  // will break VSCode's import highlights
  // "include": ["../main/ElectronApi.d.ts", "./features/**/*.ts"],
}
