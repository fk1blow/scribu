import styled from '@emotion/styled';
import React from 'react';

const StyledTabsBar = styled.div`
  height: 48px;
  background: #fffbf2c9;
`

const TabsBar: React.FC = () => {
  return (<StyledTabsBar></StyledTabsBar>)
}

export default TabsBar
